# Global configurations for YGOFabrica

# Define one or more game directories
[gamedir.$GAMEDIR_NAME]
path = '$GAMEDIR_PATH'
default = true

# Define one or more picsets
[picset.regular]
mode = 'proxy'
size = 256
field = true
default = true
